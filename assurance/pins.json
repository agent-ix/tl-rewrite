{
  "schema_version": "tl-rewrite.assurance-pins/v1",
  "purpose": "The Engineering Assurance release this repository adopts, and the digests of the artifacts it actually reads from that release. Component version pins are NOT restated here: the packaged compatibility matrix is their authority and a second copy would drift from it.",
  "matrix_authority": "engineering_assurance/compatibility-matrix.json in the installed distribution. scripts/check_shared_pins.py observes the local toolchain and delegates every verdict to engineering_assurance.compatibility.",
  "registry": "Every npm component resolves from the public npm registry (registry.npmjs.org). The internal npm.ix mirror must not appear in any requirement, pin, lockfile, or .npmrc, and is not an oracle for whether a release landed. Querying an @agent-ix package needs --@agent-ix:registry=https://registry.npmjs.org; a bare --registry does not override the scoped .npmrc entry.",
  "engineering_assurance": {
    "version": "0.2.1",
    "requirement": "engineering-assurance @ git+https://github.com/agent-ix/engineering-assurance@v0.2.1",
    "release_form": "git tag v0.2.1, a source distribution; the tag is the released artifact",
    "declared_in": "requirements-assurance.txt"
  },
  "repinned_workflow_component": {
    "component": "quoin",
    "was": "0.22.5",
    "now": "0.23.1",
    "why": "The hosted workflow pinned @agent-ix/quoin@0.22.5, which the packaged compatibility matrix names explicitly incompatible: it 'predates the `quoin change-assurance` surface; seal, intake, and receipt commands do not exist'. Every command this migration's chain runs is on that surface, so the old pin could not have worked. ix-flow@0.0.4 was absent from the workflow entirely and is now installed. quire-cli was already at the accepted 0.31.0."
  },
  "repinned_ix_flow": {
    "component": "ix-flow",
    "was": "0.0.4",
    "now": "0.2.3",
    "why": "The packaged compatibility matrix classifies ix-flow strictly at its exact pinned version, so the installed 0.2.3 against the v0.2.0 matrix (which pins 0.0.4) classified as unknown and failed the local qualification gate. engineering-assurance v0.2.1 (release: accept ix-flow 0.2.3 matrix, agent-ix/engineering-assurance#47) re-pins the matrix to ix-flow 0.2.3 with the human acceptance recorded, so this repository's engineering_assurance pin moved from v0.2.0 to v0.2.1 together with the hosted workflow literal, mirroring tl-syntax, tl-parse and tl-mltl. quire-cli and quoin stay at their already-accepted versions."
  },
  "consumed_artifacts_note": "Before agent-ix/tl-rewrite#13 this list held five entries, four of them digest-pinned: verification_semantics.py, schemas/pgm01-compatibility-view-v1.schema.json, and the two PGM-01 positive-control fixtures. Every one of those four was read by exactly one caller, scripts/legacy_evidence_view.py, which #13 deleted. Leaving them here would have claimed this repository still consumes artifacts nothing opens; removing them without replacement would have left artifact_digest_mismatches re-hashing an empty list and reporting clean, which is a check passing over no subject. Neither was acceptable, so the falsehood was fixed and the population was refilled with the module the surviving caller actually imports. compatibility-matrix.json still carries no digest, for the reason stated on its entry, so it cannot be that subject.",
  "consumed_artifacts": [
    {
      "path": "compatibility.py",
      "sha256": "c9dd4f8d0dca38a32206c114ba04304776baacfa8c1d5036691d3a46790d966d",
      "why": "scripts/check_shared_pins.py imports accepted, classify_all and load_matrix from this module, and every component verdict this repository reports comes out of it. It is a live read on every `make pins`, not a retained-evidence pin, so it survives the deletion of the compatibility view and keeps the digest re-derivation asserting over a real subject. Probed: a one-byte edit to the installed module makes `make pins` report a consumed-artifact digest mismatch and exit 1."
    },
    {
      "path": "compatibility-matrix.json",
      "why": "The authority on which component versions are pinned. Deliberately carries no digest here: it is expected to advance as the matrix is re-accepted, and pinning its bytes locally would make this repository a second acceptance authority."
    }
  ],
  "deleted_legacy_evidence": {
    "authority": "agent-ix/engineering-assurance#7, section 'Preservation constraint released for the pre-stable phase'. Decided by the repository owner on 2026-09-02. Tracked as agent-ix/tl-rewrite#13.",
    "deleted": "582 files and 3,720,647 bytes under evidence/ (11 records, ANCHORS, RETRACTIONS.json, a README and 11 .sha256 manifests); scripts/legacy_evidence_view.py; 8 files under tests/fixtures/legacy-compat/; 3 files under schemas/.",
    "what_was_measured_before_deletion": "All 11 envelopes were quire.derivation-evidence/v1 and map_pgm01_bytes classified every one 'incompatible' with the reason 'unknown PGM-01 schema version'. This repository never retained a quire.pgm01-evidence record. The figure was measured against this repository's own bytes rather than inherited: tl-syntax reported the identical refusal over 23 envelopes and tl-parse over 12, while quire-contract-ir retained real PGM-01 records that mapped and quire-analyze's Markdown summaries read as 'unreadable'. Filed upstream as agent-ix/engineering-assurance#21, which the deletion makes moot for this repository rather than fixed.",
    "schemas_proved_dead_before_deletion": "schemas/ held tl-rewrite-evidence-manifest-v1.schema.json (frozen because all 11 envelopes named its exact bytes), tl-rewrite-evidence-input-v1.schema.json (retained, and measurably NOT the artifact any envelope named), and a README. Each was grepped by filename across src/, scripts/, tests/ and assurance/ and the tree was searched for include_str! and include_bytes!: zero hits in either, and no module imports jsonschema. Nothing validated any document against either schema, so both were dead once the records that named them by digest were gone. This was checked here rather than inherited, because two sibling repositories hold schemas that look frozen by name and are live output contracts: quire-contract-codegen's pgm01-derivation-evidence-envelope-v1.schema.json is included as PGM_SCHEMA and validated on every generation, and quire-analyze's differential-report-v1.schema.json is included by src/report.rs and validated on every published document. Both were kept in those repositories.",
    "integrity_boundary": "Git history and pull-request review, which is what CONTRIBUTING.md has always declared. The deleted bytes remain recoverable from history at b1bb55a and are not restated here."
  },
  "corrected_wire_constant": {
    "constant": "TL_MLTL_REVISION",
    "was": "da2c7704a5347d063398c852acf6aa5bf9b5752d",
    "now": "b346cd0902794633e862f644a5575fc9776c34fb",
    "why": "Every ConformanceReport this crate emits carries evaluator_revision, and it once carried a revision the build did not use. scripts/check_provenance.py requires src/lib.rs, Cargo.toml and Cargo.lock to name the same durable tl-mltl revision and the same shared tl-syntax revision; TC-030 restores the former stale value to show the check goes red. The current value is tl-mltl main after PR #64 and provides the origin-complete past evaluator used by the past rewrite parity tests."
  },
  "known_drift": [
    "Resolved by the bump to v0.2.1: engineering-assurance v0.2.0 packaged compatibility-matrix.json with accepted.state = pending_human_acceptance and shipped no human_acceptance_recorded predicate (agent-ix/engineering-assurance#20). v0.2.1 (release: accept ix-flow 0.2.3 matrix, #47) carries accepted.state = accepted with accepted_by and accepted_at populated, so the record this repository reads is a release carrying the acceptance rather than a branch head.",
    "map_pgm01_bytes at the pinned release covers quire.pgm01-evidence v1 and v2 only. It has no mapping for quire.derivation-evidence/v1, which is the family this repository and its temporal siblings retained. Filed as agent-ix/engineering-assurance#21, reported and never worked around. It is no longer drift for this repository: the envelopes it applied to were deleted under agent-ix/tl-rewrite#13, so nothing here is affected either way. The upstream gap itself is unchanged."
  ],
  "upstream_dependencies": {
    "statement": "Both temporal dependencies resolve to exact remote-reachable revisions. tl-rewrite hands tl_syntax::Formula values straight to tl_mltl evaluators, so the two crates must resolve the SAME tl-syntax revision; scripts/check_provenance.py requires src/lib.rs, Cargo.toml and Cargo.lock to agree for both.",
    "tl-syntax": {
      "was": "d52d89549b0a6c0c429261bab912cd5396c4a19e",
      "now": "9de638dc4d14d0ae62a6825473a3a9bb6a9e57ac",
      "why": "9de638dc is the remote-reachable Stage 1 syntax revision with the scoped release verifier, tracked-source census, span-independent semantic identity, and coverage-test repair. It is the exact revision consumed by tl-rewrite, the pinned tl-mltl evaluator, the dev-only tl-parse, and the dev-only tl-oracle, so one tl-syntax resolves in Cargo.lock."
    },
    "tl-mltl": {
      "was": "c8d2c871dbb379019a58fe74fcb230f501088538",
      "now": "26902f03e00c674152633d99b00dee4e7aa07600",
      "why": "26902f03 is the remote-reachable Stage 1 evaluator and verification-campaign revision, including the bounded V2 exit correction and retained V5 mutation evidence, and carries the shared tl-syntax revision 9de638dc. Pinning both exact revisions keeps the Formula type passed across the crate boundary identical."
    },
    "earlier_dangling_pin": {
      "revision": "da2c7704a5347d063398c852acf6aa5bf9b5752d",
      "statement": "This repository's eleven retained evidence records named tl-mltl da2c7704, a commit reachable from no remote ref and from no tag, so it existed only on one disk. agent-ix/tl-mltl pushed origin/retain/tl-mltl-v0.1-da2c7704 to keep it fetchable while those records stood. They no longer stand: agent-ix/tl-rewrite#13 deleted all eleven. tl-mltl's own deletion, merged as 58f858e, had already removed the eight files on its side that named the revision, so these eleven records were the last references to da2c7704 anywhere in the campaign. Nothing now requires it fetchable. This change neither deletes the retention branch nor closes agent-ix/tl-mltl#11; both are handled once all eight deletions have landed. What this repository states is only that it has removed the last reference. It never pinned the revision for the build: the constant that used to name it was stale against Cargo.toml and is corrected below."
    }
  }
}
