# Native Engineering Assurance source and installation contract.
# The v0.4.1 tag carries Peter Krenesky's accepted compatibility matrix.
# This file remains a tracked mirror-scan input. Historical Python requirements
# were removed because engineering_assurance.compatibility no longer exists.
#
# Install exactly:
# cargo +1.98.1 install --git https://github.com/agent-ix/engineering-assurance --tag v0.4.1 --locked --bin engineering-assurance
#
# The executable embeds the matrix. scripts/check_shared_pins.py observes the
# installed CLI and validates its classification against the declared release.
# The gate withholds when any observed component is absent, unknown, or incompatible.
