---
id: CAC-001
title: tl-rewrite deterministic transformation contract
type: ComponentAssuranceContract
status: active
owner: tl-rewrite-maintainer
kind: deterministic
responsibility: produce bounded profile-preserving rewrites and independently checkable context-bound evidence
inputs: [validated formula, immutable rule catalog, stable strategy, explicit budgets, optional equivalence domain, optional shared signal catalog and requirement context]
outputs: [normalized formula and trace, explicit non-success report, replay result, conformance report]
invariants: [semantic profile never changes, every change names one rule, partial work is never successful, every surviving proposition retains its supplied signal binding]
failure_behaviors: [reject schema drift, reject resource excess, reject unresolved contextual bindings, retain mismatch, return non-conclusive]
version_pins:
  rust-msrv: "1.75"
  catalog: tl-rewrite.catalog/v1
  rewrite-schema: tl-rewrite.report/v1
  contextual-rewrite-schema: tl-rewrite.report/v2
  contextual-replay-schema: tl-rewrite.replay/v2
  conformance-schema: tl-rewrite.conformance/v1
  contextual-conformance-schema: tl-rewrite.conformance/v2
controls:
  surfaces: [validated tl-syntax Formula, shared tl-syntax signal and requirement-context documents, immutable rule catalog, bounded rewrite API, exact replay API, tl-mltl comparison API]
  fallback: return an explicit non-success report without normalized output
  abstention: do not enable or claim equivalence for unsupported or incompletely enumerated cases
  escalation: require human review for rule, profile, dependency, or corpus changes
isolation: parser, external prover, production monitor, and consuming project remain external
replacement: preserve rule identities, profiles, deterministic ordering, error taxonomy, corpus, and evidence digests
relationships:
  - target: ix://agent-ix/tl-rewrite/AP-001
    type: references
---

# tl-rewrite deterministic transformation contract

## Component Boundary

The component begins with a validated formula and explicit rule catalog/options.
Contextual calls additionally begin with a shared signal catalog and optional
shared requirement context. It ends with a typed native report. Text parsing,
contract-IR translation, generic evidence retention, and downstream execution
remain outside.

## Required Behavior

Stable rule order and budgets determine every result. Successful reports carry
a valid fixed-point formula and complete step chain. Bounded equivalence names
the exact enumerated domain and evaluator. A contextual success additionally
binds the complete signal catalog, preserves exact caller context, and resolves
every surviving proposition through the unchanged catalog.

## Failure Handling

Schema/profile mismatch, budget exhaustion, graph growth, cycles, replay drift,
unresolved proposition bindings, enumeration excess, and semantic mismatch are
distinct non-success outcomes.

## Controls

Checked counters, deterministic logical work, graph interning, cycle digests,
exact dependency pins, strict schemas, corpus checksums, and independent replay
gate all exchanged claims.

## Replacement

A replacement must pass the same catalog, WEST subset, adversarial budgets,
replay, exhaustive bounded comparison, and shared-assurance intake gates.

The last of those said "retained evidence gates" until issue #13. There are no
retained-evidence gates now — the archive and its reader are deleted — so the
clause named a gate no replacement could ever pass. It is replaced by the gate
set that actually runs, `make assurance`, rather than left as an unsatisfiable
condition or dropped without saying why.
