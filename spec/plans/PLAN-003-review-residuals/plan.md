---
id: PLAN-003
title: "Close post-merge census and probe residuals"
type: Plan
status: complete
relationships:
  - target: ix://agent-ix/tl-rewrite/FR-006
    type: references
---
# PLAN-003: Close post-merge census and probe residuals

## Objective

Close the concrete, repository-scoped residuals recorded in issue #19 after the
reviews of PRs #17 and #18, without claiming that this repository qualifies
Make or recreating a repository-local assurance framework.

## Work

1. Make Git enumeration reusable by the live census and a real fixture
   repository; prove enumeration failure, hostile template exclusions, local
   excludes isolation, preferred `GNUmakefile`, and `.yaml` workflow coverage.
2. Cross-check executable denial and deleted-name sets against an explicitly
   unsealed declaration block, widen `compat-view` to the plain target name, and
   constrain the complete historical-prose exemption population.
3. Reconcile FR-006-AC-7 across the requirement, matrix, and declaration.
4. Remove the dead post-creation symlink assertion, resolve an existing real
   Quoin store leaf before containment comparison, and improve probe diagnostics.
5. Record the reviewed-head transfer basis for the earlier anti-flake campaign,
   then run the focused controls and the full local gate at the final head.

## Boundary

FND-1810 and the wider Make execution-control class remain with
`agent-ix/tl-rewrite#11` and `agent-ix/engineering-assurance#11`. This plan adds
no Make parser, runner, or qualification claim. Hosted CI remains manual-only
and is not dispatched by this plan.
